// Legacy tour data placeholder to satisfy default import in TourDetails.jsx
// If you use this page, populate with objects like { id, photo, title, desc, price, reviews, city, distance, maxGroupSize, availableDates, avgRating, features }
const tourData = [];

export default tourData;


